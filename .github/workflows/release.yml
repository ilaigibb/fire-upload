name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 9.15.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm build
      - name: Package release
        run: |
          mkdir release
          cp dist/server.js release/
          cp deploy/install.sh release/
          cp deploy/update.sh release/
          cp deploy/fire-upload.service release/
          cp deploy/fire-upload-update.service release/
          cp deploy/fire-upload-update.timer release/
          cp deploy/duckdns-update.sh release/
          cp deploy/duckdns-update.service release/
          cp deploy/duckdns-update.timer release/
          cp deploy/proxmox.sh install-fire-upload.sh
          printf '%s\n' "$GITHUB_REF_NAME" > release/VERSION
          tar -C release -czf fire-upload.tar.gz .
      - env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" fire-upload.tar.gz install-fire-upload.sh --generate-notes --verify-tag
