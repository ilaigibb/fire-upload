interface:
  display_name: "Fire Upload"
  short_description: "Upload PR artifacts to a public file host"
  default_prompt: "Use $fire-upload to upload this local artifact and format its public URL for a pull request."
